# https://github.com/argoproj/applicationset/issues/553
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: {{ .Values.spec.project }}-servers
  namespace: argocd
spec:
  generators:
  - git:
      repoURL: https://github.com/rlaisqls/xquare-gitops-repo-staging.git
      revision: HEAD
      files:
      - path: "resource/{{ .Values.spec.environment }}/**/resource.json"
  template:
    metadata:
      name: '{{`{{ config.Name }}`}}-{{`{{ config.Type }}`}}-{{`{{ config.Environment }}`}}'
    spec:
      destination:
        server: {{ .Values.spec.destination.server }}
        namespace: xquare
      project: {{ .Values.spec.project }}
      source:
        path: {{ .Values.spec.source.path }}
        repoURL: {{ .Values.spec.source.repoURL }}
        targetRevision: {{ .Values.spec.source.targetRevision }}
        helm:
          values: |
            {{`{{ config | toYaml }}`}}
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
